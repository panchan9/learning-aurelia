# learning-aurelia

## Installation
* run the follwoing command in the project root.
`npm install`
* move the API package directory.
`cd api`
* install the dependency
`npm install`

## Launch the app
`au run --watch`
